import { StyleSheet } from "react-native";

export const LoaderStyle = StyleSheet.create({
  loaderLoacation: {
    flex: 1,
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
  },
});
